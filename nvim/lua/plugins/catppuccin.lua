require("catppuccin").setup({
  integrations = {
    telescope = true,
    treesitter = true,
    treesitter_context = true,
    nvimtree = true,

    indent_blankline = {
      enabled = true,
      colored_indent_levels = true
    }
  }
})

