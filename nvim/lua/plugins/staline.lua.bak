require("packages")

require("staline").setup{
  sections = {
    left = {
      " ", "file_size", " ", "file_name", " ",
      branch, " "
    },
    mid  = {"lsp"},
    right= {
      "cool_symbol", "lsp_name", "line_column",  " "
    }
  },

  defaults={
    fg = mocha.base,
    cool_symbol = "  ",
    true_colors = false,
    line_column = "[%l:%c] %p%% ",
  },

  mode_colors = {
    n  = mocha.mauve,
    i  = mocha.green,
    c  = mocha.sapphire,
    v  = mocha.yellow
  }
}

